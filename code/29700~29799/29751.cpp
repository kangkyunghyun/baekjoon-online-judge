#include <bits/stdc++.h>
using namespace std;

int main() {
  cin.tie(0)->sync_with_stdio(0);
  int w, h;
  cin >> w >> h;
  cout.precision(1);
  cout << fixed;
  cout << (double)w * h / 2;
  return 0;
}