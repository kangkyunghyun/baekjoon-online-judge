#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    string x;
    cin >> n;
    while(n--){
        cin >> x;
        if (x.find('S')!=-1)
        {
            cout<<x;
            continue;
        }
    }
}