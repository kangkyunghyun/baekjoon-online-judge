#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);
  long long n;
  cin >> n;
  cout << (n + 1) * n / 2 << '\n';
  cout << (int)pow((n + 1) * n / 2, 2) << '\n';
  cout << (int)pow((n + 1) * n / 2, 2) << '\n';
  return 0;
}