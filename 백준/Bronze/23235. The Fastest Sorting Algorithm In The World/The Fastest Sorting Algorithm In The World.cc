#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);
  string s;
  int cnt = 0;
  while (getline(cin, s) && s != "0") {
    cnt++;
    cout << "Case " << cnt << ": Sorting... done!\n";
  }
  return 0;
}