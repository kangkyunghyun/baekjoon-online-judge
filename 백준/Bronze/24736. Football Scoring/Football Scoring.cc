#include <iostream>
using namespace std;

int main() {
  int T, F, S, P, C;
  cin >> T >> F >> S >> P >> C;
  cout << T * 6 + F * 3 + S * 2 + P + C * 2 << ' ';
  cin >> T >> F >> S >> P >> C;
  cout << T * 6 + F * 3 + S * 2 + P + C * 2 << '\n';
  return 0;
}