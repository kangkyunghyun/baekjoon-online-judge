#include <bits/stdc++.h>
using namespace std;
 
int main() {
  cin.tie(0)->sync_with_stdio(0);
  int t;
  cin >> t;
  while (t--) {
    int n, k;
    cin >> n >> k;
    int x;
    string ans = "NO";
    for (int i = 0; i < n; i++) {
      cin >> x;
      if (x == k)
        ans = "YES";
    }
    cout << ans << '
';
  }
  return 0;
}