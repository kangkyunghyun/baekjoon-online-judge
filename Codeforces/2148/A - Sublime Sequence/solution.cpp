#include <bits/stdc++.h>
using namespace std;
#define int long long
 
signed main() {
    cin.tie(0)->sync_with_stdio(0);
    int t;
    cin >> t;
    while (t--) {
        int x, n;
        cin >> x >> n;
        if (n % 2)
            cout << x;
        else
            cout << 0;
        cout << '
';
    }
}